{
  "name": "xross-shared",
  "version": "1.0.0-beta.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "commit": "yarn --cwd ../.. commit",
    "cz": "yarn commit",
    "build": "tsc && tscpaths -p tsconfig.json --src ."
  }
}
