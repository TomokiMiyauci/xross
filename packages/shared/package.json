{
  "name": "xross-shared",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "commit": "yarn --cwd ../.. commit",
    "cz": "yarn commit",
    "build": "tsc && tscpaths -p tsconfig.json --src ."
  }
}
