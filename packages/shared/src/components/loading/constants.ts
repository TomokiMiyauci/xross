export const DEFAULT_STROKE = 6
export const DEFAULT_RADIUS = 32
export const DEFAULT_COLOR = '#3051ff'
export const PATH_LENGTH = 100
export const COLOR_CLASS = 'color'
import s from './index.module.less'
export const classNames = {
  loading: s.loading,
  circle: s.circle,
  color: {
    neutral: s.neutral,
    primary: s.primary,
    success: s.success,
    warning: s.warning,
    danger: s.danger
  }
}
